1. Kvm4JSdk 说明
/**
 * Starts a  session for the specified device and channel.
 *
 * @param ip The IP address of the device.
 * @param port The port number of the device.
 * @param channelNo The channel number to connect to.
 * @param account The account for authentication.
 * @param password The password for authentication.
 * @param timeout 超时等待时间,单位为毫秒
 * @throws Exception If the channel number is less than 0.
 * @since 1.3.4.1
 */
 public static KVMCInstance connectKVMC(String ip, int port, int channelNo,String account, String password,long timeout) throws Exception;

2. KVMCInstance API说明
/**
  * API:停止会话
  */
void stopSession();

/**
  * API:发送绝对鼠标事件
  * @param x x坐标
  * @param y y坐标
  * @param mask 按键掩码
  * 采用位掩码（Bitmask） 来编码鼠标事件。
  * 每个按键或滚轮动作都被赋予一个唯一的二进制位。
  * 你可以通过位或（OR）运算 | 来组合多个动作
  *     例如，同时按下左键和右键对应的掩码是 1 | 4 = 5.松开左键只按右键是 0 | 4 = 4
  * 1.左键按下:1
  * 2.左键松开:0
  * 3.右键按下:4
  * 4.右键松开:0
  * 5.中键按下:2
  * 6.中键松开:0
  * 7.滚轮向上:8
  * 8.滚轮向下:16
  * 9.滚轮向左:32
  * 10.滚轮向右:64
  * 特别说明:每接收鼠标滚轮被旋转的"咔哒声"一次,就需要发送一次滚轮事件.
  */
WriteFuture sendABSMouseEvent(x, y, mask);

/**
 * API:发送相对鼠标事件
 * @param x x相对位移
 * @param y y相对位移
 * @param mask 按键掩码
 */
WriteFuture sendRELMouseEvent(x,y,mask);

/**
  * API:设置KVM设备为绝对鼠标模式
  * 绝对鼠标模式下，既可以发送绝对鼠标事件，也可以发送相对鼠标事件
  */
WriteFuture setABSMouseType();

/**
 * API:设置KVM设备为相对鼠标模式
 * 相对鼠标模式下，既可以发送相对鼠标事件，也可以发送绝对鼠标事件
 */
 WriteFuture setRELMouseModel();

/**
  * API:发送键盘事件
  * @param keyCode 键盘按键码
  * @param pressed 键盘按键按下状态 1按下 0释放
  */
WriteFuture sendKeyEvent(pressed,keyCode);

/**
  * API:发送字符串
  * @param chars 英文输入法字符串
  * 正则: ^[a-zA-Z0-9\s!"#$%&'()*+,-./:;<=>?@\[\\\]^_`{|}~ ]*$
  */
void autoSendEnChars(chars) throws Exception;

/**
  * API:设置连接断开回调
  * @param kvmcCloseCallBak
  */
void setKvmCCloseCallBak(KVMCCloseCallBak kvmcCloseCallBak)

/**
  * API:设置图片流回调
  * @param kvmCAVFrameCallBack
  */
void setKvmCAVFrameCallBack(KVMCAVFrameCallBack kvmCAVFrameCallBack)

/**
  * API:设置H264裸流回调
  * @param kvmcRealRawCallBack
  */
void setKvmCRealRawCallBack(KVMCRealRawCallBack kvmcRealRawCallBack)