═══════════════════════════════════════════════════════════════════════════════
                       KVM 鼠标测试修复 - 快速对比
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 修改前 (错误)                                                                │
└─────────────────────────────────────────────────────────────────────────────┘

  坐标处理：
    ❌ 归一化坐标: pixel_x / video_width * 65535
    ❌ 归一化坐标: pixel_y / video_height * 65535
  
  发送方法：
    ❌ client.send_mouse_event(norm_x, norm_y, 0, mouse_type=1)
  
  结果：
    ❌ 可以截图，但鼠标位置不对
    ❌ 截图中看不到鼠标或位置错误

┌─────────────────────────────────────────────────────────────────────────────┐
│ 修改后 (正确)                                                                │
└─────────────────────────────────────────────────────────────────────────────┘

  坐标处理：
    ✅ 直接像素坐标: pixel_x (0 到 width-1)
    ✅ 直接像素坐标: pixel_y (0 到 height-1)
  
  发送方法：
    ✅ client.send_mouse_event_raw(pixel_x, pixel_y, 0)
  
  结果：
    ✅ 可以截图，鼠标位置正确
    ✅ 截图中可以清楚看到鼠标在正确位置

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 代码示例对比                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

╔═════════════════════════════════════════════════════════════════════════════╗
║ 异步版本 (test_mouse_with_screenshot.py)                                    ║
╚═════════════════════════════════════════════════════════════════════════════╝

  修改前：
    ────────────────────────────────────────────────────────────────────────
    def to_normalized(pixel_x: int, pixel_y: int) -> tuple:
        norm_x = int(pixel_x / video_width * 65535)
        norm_y = int(pixel_y / video_height * 65535)
        return (norm_x, norm_y)
    
    nx, ny = to_normalized(pixel_x, pixel_y)
    client.send_mouse_event(nx, ny, 0, mouse_type=1)
    ────────────────────────────────────────────────────────────────────────

  修改后：
    ────────────────────────────────────────────────────────────────────────
    # 直接使用像素坐标
    test_positions = [
        ("左上角", 50, 50),
        ("中心", video_width // 2, video_height // 2),
    ]
    
    client.send_mouse_event_raw(pixel_x, pixel_y, 0)
    ────────────────────────────────────────────────────────────────────────

╔═════════════════════════════════════════════════════════════════════════════╗
║ 同步版本 (test_sync_mouse_screenshot.py)                                    ║
╚═════════════════════════════════════════════════════════════════════════════╝

  修改前：
    ────────────────────────────────────────────────────────────────────────
    # 已经使用 send_mouse_event_raw，但注释不清晰
    client.send_mouse_event_raw(pixel_x, pixel_y, 0)
    ────────────────────────────────────────────────────────────────────────

  修改后：
    ────────────────────────────────────────────────────────────────────────
    # 添加了清晰的注释和调试信息
    x_hex = f"0x{(pixel_x >> 8) & 0xFF:02X}{pixel_x & 0xFF:02X}"
    y_hex = f"0x{(pixel_y >> 8) & 0xFF:02X}{pixel_y & 0xFF:02X}"
    print(f"  🔢 坐标十六进制: X={x_hex}, Y={y_hex}")
    
    client.send_mouse_event_raw(pixel_x, pixel_y, 0)
    ────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 测试位置示例                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  视频分辨率: 1920x1080

  ┌─────────────────┬──────────────┬──────────────┬──────────────────┐
  │ 位置            │ 像素坐标     │ 十六进制     │ 数据包 (hex)     │
  ├─────────────────┼──────────────┼──────────────┼──────────────────┤
  │ 原点            │ (0, 0)       │ 0x0000/0x0000│ 05 00 00 00 00 00│
  │ 左上角          │ (50, 50)     │ 0x0032/0x0032│ 05 00 00 32 00 32│
  │ 中心            │ (960, 540)   │ 0x03C0/0x021C│ 05 00 03 C0 02 1C│
  │ 右下角          │ (1870, 1030) │ 0x074E/0x0406│ 05 00 07 4E 04 06│
  └─────────────────┴──────────────┴──────────────┴──────────────────┘

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 修改文件清单                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  测试代码:
    ✅ backend/tests/test_mouse_with_screenshot.py           [异步版本]
    ✅ backend/sync_tests/test_sync_mouse_screenshot.py      [同步版本]
  
  文档:
    📝 backend/tests/README_鼠标测试修复.md                   [详细说明]
    📝 backend/tests/README_同步异步对比.md                   [版本对比]
    📝 backend/tests/CHANGELOG_鼠标测试修复.md                [变更日志]
    📝 backend/tests/修复对比.txt                             [本文件]

  客户端代码:
    ⚠️  未修改 - 底层实现已经是正确的

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 运行测试                                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

  异步版本:
    $ cd /Users/liu/Work/sophon/sophon-demo/kvm-rpa/backend
    $ python tests/test_mouse_with_screenshot.py

  同步版本:
    $ cd /Users/liu/Work/sophon/sophon-demo/kvm-rpa/backend
    $ python sync_tests/test_sync_mouse_screenshot.py

  自定义参数:
    $ python sync_tests/test_sync_mouse_screenshot.py \
        --ip 192.168.0.100 \
        --port 5900 \
        --channel 0 \
        --username admin \
        --password 123456 \
        --debug

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 关键要点                                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

  1. ✅ 不要进行坐标归一化
  2. ✅ 直接使用像素坐标（0 到 width-1, 0 to height-1）
  3. ✅ 使用 send_mouse_event_raw() 方法
  4. ✅ 与 Java SDK ViewerSample.encodeMouseEvent 完全一致
  5. ✅ 设备端负责解释坐标（根据当前鼠标模式）

═══════════════════════════════════════════════════════════════════════════════
                                  修复完成
═══════════════════════════════════════════════════════════════════════════════










