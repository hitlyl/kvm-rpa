流程节点需要增加 python自定义代码节点，即可以上传一段python代码，根据上一节点的输出，自定义一些逻辑处理，然后输出到下一节点。python代码需要考虑到安装依赖的python包。可以单独配置一下依赖，然后点击安装，后台即进行安装（后台是否需要重启？）
yolo检测节点用到的模型，需要有上传的地方，然后在配置yolo节点的时候，直接选择即可。
图像裁剪和图像预处理节点，应该更友好一些，例如裁剪可以弹出个框，先获取当前流程中的kvm的图片，然后在图片上画裁剪框。
点击每个节点时，除了可以编辑属性，最好有个说明页，说明一下节点的输入输出格式等。
当设置连线说明后，重新加载图的时候，线上没有呈现说明文字，这是个bug，需要修复。